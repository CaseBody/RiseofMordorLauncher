<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:RiseofMordorLauncher.Directory.ItemTemplates">

    <DataTemplate x:Key="SubmodTemplate">
        <Grid Background="Black" 
              Margin="10">
            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0"
                    BorderThickness="2"
                    BorderBrush="Wheat"
                    Grid.Column="0"
                    Grid.RowSpan="3">

                <Image Source="{Binding ImagePath}"/>

            </Border>

            <TextBlock Foreground="Wheat" 
                       Grid.Row="0"
                       Grid.Column="1"
                       Padding="5"
                       Text="{Binding Name, FallbackValue=Submod}"/>

            <TextBlock Foreground="Wheat" 
                       Grid.Row="1"
                       Grid.Column="1"
                       Padding="5"
                       Text="{Binding SubmodSteamId, FallbackValue=Id}"/>

            <Button Grid.Row="2"
                    Grid.Column="1"
                    Background="Green"
                    FontWeight="Bold"
                    FontSize="24"
                    Style="{StaticResource DarkButton}"
                    Content="Subscribe" />

        </Grid>
    </DataTemplate>

</ResourceDictionary>